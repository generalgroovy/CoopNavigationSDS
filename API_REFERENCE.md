# Package API Reference

Generated by `python scripts/generate_research_docs.py`.
The inventory is produced with Python AST parsing, so generation does not import model, TTS, or ASR providers.

## `coop_navigation_sds/__init__.py`

CoopNavigationSDS cooperative transit speech-dialog research platform.

No public runtime components are defined in this module.

## `coop_navigation_sds/__main__.py`

Run an interactive experiment or a deterministic smoke experiment.

No public runtime components are defined in this module.

## `coop_navigation_sds/app.py`

Interactive startup controller for one headless speech-dialog experiment.

### Class `ConsoleEventSink`

Print a readable speech transcript and retrospective metric report.

- `ConsoleEventSink.__init__(self, view='compact')`: Internal class operation.
- `ConsoleEventSink.put(self, event)`: Internal class operation.
- `ConsoleEventSink._print_speech_turn(self, payload)`: Internal class operation.
- `ConsoleEventSink._print_transcript_row(label, value)`: Internal class operation.
- `ConsoleEventSink._print_memory_state(self, payload)`: Internal class operation.
- `ConsoleEventSink._format_memory_additions(additions)`: Internal class operation.
- `ConsoleEventSink._format_task_variables(memory)`: Internal class operation.
- `ConsoleEventSink._format_token_changes(changes, quote_source=False)`: Internal class operation.
- `ConsoleEventSink._print_run_summary(summary)`: Internal class operation.
- `ConsoleEventSink._print_system_message(self, message)`: Internal class operation.
- `ConsoleEventSink._print_debug_event(label, payload)`: Internal class operation.
- `ConsoleEventSink._print_configuration(config)`: Internal class operation.
- `ConsoleEventSink._print_metric_dependency_overview(report)`: Internal class operation.
- `ConsoleEventSink._print_metric_results(metric)`: Internal class operation.
- `ConsoleEventSink._format_metric_value(value)`: Internal class operation.

### Function `build_agent_a_responder(model_adapter, llm_agent_a=LLM_AGENT_A, agent_a_type=None)`

Create the configured Agent A responder.

### Function `agent_a_model_integrity(config, model_adapter)`

Return explicit Agent A model-use evidence for protocol output.

### Function `configure_model_adapter_runtime(model_adapter, calculation_max_time_sec)`

Apply a local generation budget without changing service timeouts.

### Function `default_run_config()`

Return the complete default configuration for one speech experiment.

### Function `normalize_run_config(config)`

Validate and complete a mandatory full-speech run configuration.

### Function `select_run_config()`

Show the startup-only configuration window.

### Function `_require_module(module_name, implementation)`

Internal module operation.

### Function `_provider_python(config, stage, engine)`

Internal module operation.

### Function `_require_module_or_provider(config, stage, engine, module_name, implementation)`

Internal module operation.

### Function `_validate_transformers_model(model_name, allow_download)`

Confirm that an offline local-model selection can actually be loaded.

### Function `validate_run_config_for_start(config)`

Validate implementation-specific requirements before closing the GUI.

### Function `configured_test_case(run_config)`

Build the standardized case with all caller and stage overrides.

### Function `prepare_execution_run_config(run_config)`

Create paths once and freeze the complete runtime specification.

### Function `build_dialog_runtime(event_queue, model_adapter, run_config)`

Build agents, speech stages, and dialogue orchestration.

### Function `_segment(event_queue, name, **payload)`

Internal module operation.

### Function `conversation_worker(event_queue, model_adapter, run_config)`

Execute one full speech dialogue and write all research outputs.

### Function `_load_model_or_fallback(run_config, event_queue)`

Internal module operation.

### Function `main()`

Configure and execute one experiment without a runtime GUI.

## `coop_navigation_sds/AutomaticSpeechRecognition/__init__.py`

Automatic speech recognition engines and transcript adapters.

No public runtime components are defined in this module.

## `coop_navigation_sds/AutomaticSpeechRecognition/engines.py`

Public automatic speech recognition component API.

No public runtime components are defined in this module.

## `coop_navigation_sds/batch.py`

Batch experiment entry point for automatic evaluation of speech-dialog conditions.

### Function `parse_csv_arg(value, all_values=None)`

Parse a comma-separated CLI argument into a list.

### Function `parse_bool_flag(value)`

Parse CLI booleans.

### Function `preflight_agent_b_model_grid(agent_b_config, provider, base_url, conditions, timeout_sec, models_dir=None)`

Verify every model-backed Agent B condition before batch artifacts exist.

### Function `main()`

Run the configured experiment grid and write metrics output.

## `coop_navigation_sds/Configuration/__init__.py`

Configuration defaults and the optional startup configuration window.

No public runtime components are defined in this module.

## `coop_navigation_sds/Configuration/assets.py`

Canonical local asset resolution shared by preflight and runtime.

### Function `resolve_project_asset_path(value)`

Resolve project-managed relative asset paths independently of the shell cwd.

### Function `resolve_faster_whisper_model(value)`

Return the exact local CTranslate2 model directory when discoverable.

### Function `faster_whisper_model_ready(value)`

Return readiness and the canonical Faster-Whisper model location.

## `coop_navigation_sds/Configuration/component_catalog.py`

Selectable experiment component catalog for GUI and batch tooling.

### Function `_with_current_choice(values, current)`

Return values plus the current setting, preserving order and uniqueness.

### Function `speech_engine_profile(stage, engine)`

Return an independent copy of one backend's local runtime defaults.

### Function `resolve_prepared_asset_path(value)`

Anchor project-managed provider paths independently of process cwd.

### Function `apply_speech_engine_profiles(config, *, replace=False)`

Complete a configuration without carrying settings across backends.

### Function `startup_choices(extra_agent_b_plugin=AGENT_B_PLUGIN, config=None, *, operational_only=False)`

Return implementation choices, optionally restricted to ready providers.

## `coop_navigation_sds/Configuration/experiment.py`

Immutable resolved experiment specifications and provenance.

### Function `freeze_value(value)`

Recursively convert configuration data into immutable containers.

### Function `thaw_value(value)`

Return a JSON-compatible copy of recursively frozen data.

### Function `configuration_fingerprint(values)`

Return a stable SHA-256 identity for resolved non-secret values.

### Class `ExperimentSpecification`

Read-only configuration used by every phase of one experiment run.

- `ExperimentSpecification.resolve(cls, values, *, source='runtime')`: Internal class operation.
- `ExperimentSpecification.__getitem__(self, key)`: Internal class operation.
- `ExperimentSpecification.__iter__(self)`: Internal class operation.
- `ExperimentSpecification.__len__(self)`: Internal class operation.
- `ExperimentSpecification.to_dict(self)`: Internal class operation.
- `ExperimentSpecification.provenance(self)`: Internal class operation.

### Function `ensure_experiment_specification(values, *, source='runtime')`

Return an existing specification or resolve one immutable snapshot.

## `coop_navigation_sds/Configuration/experimental_defaults.py`

Research-oriented defaults shared by GUI, scripts, and batch runs.

### Function `numeric_range(key, fallback)`

Return a sane GUI slider range for a numeric setting.

## `coop_navigation_sds/Configuration/gui.py`

Startup-only Tk configuration window.

### Class `ToolTip`

Small hover tooltip for configuration controls.

- `ToolTip.__init__(self, widget, text)`: Internal class operation.
- `ToolTip.show(self, _event=None)`: Internal class operation.
- `ToolTip.hide(self, _event=None)`: Internal class operation.

### Class `StartupConfigDialog`

Collect one interactive run configuration.

- `StartupConfigDialog.__init__(self, choices, defaults, validator=None)`: Internal class operation.
- `StartupConfigDialog._build(self)`: Internal class operation.
- `StartupConfigDialog._apply_fullscreen(self)`: Internal class operation.
- `StartupConfigDialog._font_family(self)`: Choose an installed, readable UI font without assuming an operating system.
- `StartupConfigDialog._scroll_wheel(canvas, event)`: Normalize Windows, X11, and Wayland mouse-wheel event magnitudes.
- `StartupConfigDialog._build_combined_pipeline(self, parent)`: Internal class operation.
- `StartupConfigDialog._scrollable_card(self, parent, key, title)`: Internal class operation.
- `StartupConfigDialog._phase_section(self, parent, row, key, title)`: Internal class operation.
- `StartupConfigDialog._attach_phase_metrics(self, card, phase_key, row)`: Internal class operation.
- `StartupConfigDialog._expand_phase_metrics(self, host, phase_key)`: Internal class operation.
- `StartupConfigDialog._collapse_phase_metrics(self, phase_key)`: Internal class operation.
- `StartupConfigDialog._build_logging_controls(self, parent, row)`: Internal class operation.
- `StartupConfigDialog._phase_grid(self, parent)`: Internal class operation.
- `StartupConfigDialog._collapsible(self, parent, row, title)`: Internal class operation.
- `StartupConfigDialog._schedule_pipeline_refresh(self, *_args)`: Internal class operation.
- `StartupConfigDialog._current_config_snapshot(self)`: Internal class operation.
- `StartupConfigDialog._refresh_pipeline_overview(self)`: Internal class operation.
- `StartupConfigDialog._update_route_layer_options(self, config=None)`: Internal class operation.
- `StartupConfigDialog._update_selected_route_preview(self)`: Internal class operation.
- `StartupConfigDialog._draw_network_preview(self)`: Internal class operation.
- `StartupConfigDialog._refresh_dependency_tree(self, report)`: Internal class operation.
- `StartupConfigDialog._help(self, widget, key, text=None)`: Internal class operation.
- `StartupConfigDialog._combo(self, parent, row, label, key, values, editable=False, on_change=None)`: Internal class operation.
- `StartupConfigDialog._number(self, parent, row, label, key, minimum, maximum, increment=1)`: Internal class operation.
- `StartupConfigDialog._entry(self, parent, row, label, key, show=None)`: Internal class operation.
- `StartupConfigDialog._check(self, parent, row, label, key)`: Internal class operation.
- `StartupConfigDialog._clear_frame(frame)`: Internal class operation.
- `StartupConfigDialog._refresh_agent_selection(self)`: Internal class operation.
- `StartupConfigDialog._refresh_speech_selection(self)`: Internal class operation.
- `StartupConfigDialog._select_audio_persona(self)`: Internal class operation.
- `StartupConfigDialog._select_tts_engine(self)`: Internal class operation.
- `StartupConfigDialog._select_asr_engine(self)`: Internal class operation.
- `StartupConfigDialog._refresh_conditional_sections(self)`: Internal class operation.
- `StartupConfigDialog._rebuild_model_settings(self)`: Internal class operation.
- `StartupConfigDialog._rebuild_tts_settings(self)`: Internal class operation.
- `StartupConfigDialog._rebuild_asr_settings(self)`: Internal class operation.
- `StartupConfigDialog._refresh_persona_detail(self)`: Internal class operation.
- `StartupConfigDialog._select_model_provider(self)`: Internal class operation.
- `StartupConfigDialog._select_model_profile(self)`: Internal class operation.
- `StartupConfigDialog._build_metric_row(self, group, row, key, label)`: Internal class operation.
- `StartupConfigDialog.start(self)`: Internal class operation.
- `StartupConfigDialog.cancel(self)`: Internal class operation.
- `StartupConfigDialog.show(self)`: Internal class operation.

## `coop_navigation_sds/Configuration/jobs.py`

Load scriptable batch experiment job files.

### Function `load_experiment_job(path, _seen=None)`

Load and minimally validate a JSON ``.job`` experiment definition.

### Function `job_grid_value(job, key, fallback)`

Return a comma-separated command-line default from a job grid.

### Function `numeric_range_values(spec)`

Expand an inclusive numeric range with stable decimal arithmetic.

### Function `job_parameter_grid(job)`

Return arbitrary condition parameters from value lists and ranges.

### Function `job_parameter_profiles(job)`

Return linked named treatments without forming a parameter cross product.

### Function `job_linked_profiles(job)`

Return named linked factors whose fields move together as one treatment.

## `coop_navigation_sds/Configuration/metrics.py`

Evaluation-layer configuration.

No public runtime components are defined in this module.

## `coop_navigation_sds/Configuration/model_matrix.py`

Canonical Agent B model-size treatments used by research job matrices.

### Class `ModelSizeTreatment`

One controlled model-size tier with two family-diverse local models.

### Function `model_store_platform(system_name=None)`

Return the stable project-folder key for a supported operating system.

### Function `agent_b_model_platform_dir(system_name=None, *, project_root=PROJECT_ROOT)`

Return the platform-specific root containing model data and its catalog.

### Function `agent_b_ollama_store_dir(system_name=None, *, project_root=PROJECT_ROOT)`

Return the platform-specific Ollama blob and manifest store.

### Function `resolve_agent_b_model_store(path=None, system_name=None, *, project_root=PROJECT_ROOT)`

Resolve an optional model-store setting against the repository root.

### Function `model_catalog_folder(model_name)`

Return a size-first, stable catalog path for one registered model.

### Function `models_for_size_treatments(treatment_keys)`

Resolve ordered model names for one or more declared size treatments.

### Function `model_size_treatment(model_name)`

Return the configured size-treatment key for a model, if registered.

## `coop_navigation_sds/Configuration/pipeline.py`

Shared configuration-pipeline, logging, and metric-dependency model.

### Function `_transformers_assets_ready(model_name)`

Check config, tokenizer, and weights without importing Transformers.

### Function `_speech_assets_ready(key, model)`

Validate the minimum local asset signature required by a speech engine.

### Function `_metric_phase(metric_key)`

Internal module operation.

### Function `metric_required_fields(metric_key)`

Return concrete immutable trace fields required by one metric.

### Function `collected_data_fields(config)`

Resolve which data fields the selected run can actually capture.

### Function `metric_dependency_report(config)`

Return metric-by-field availability used by GUI, preflight, and manifests.

### Function `serializable_metric_dependency_report(config)`

Return the dependency report in stable JSON-ready form.

### Function `experiment_pipeline_contract(config)`

Return the selected phase contract and evidence readiness for manifests.

### Class `ComponentStatus`

Class component; see its typed members and call sites.

### Function `component_status(kind, key, config=None)`

Return offline availability without importing or downloading models.

### Function `optimal_route_preview(config)`

Calculate a concise selected-condition baseline for the startup GUI.

### Function `route_layer_comparison(layers, selected_index)`

Return edge and cost differences between one optimum and its predecessor.

## `coop_navigation_sds/Configuration/run_identity.py`

Compact run identifiers with full meaning retained in the run manifest.

### Function `naming_scheme_document()`

Return the abbreviation registry persisted with result artifacts.

### Function `write_naming_scheme(output_dir)`

Write or update the naming-scheme registry in a result directory.

### Function `compact_code(value, width=5)`

Return a deterministic readable code for an unregistered value.

### Function `single_run_label(config)`

Encode the main independent variables without long folder names.

### Function `batch_run_label(condition_count)`

Internal module operation.

## `coop_navigation_sds/Configuration/runtime.py`

Controller-layer defaults with environment-variable compatibility.

### Function `_environment(name, legacy_name, default)`

Internal module operation.

### Function `_environment_bool(name, legacy_name, default)`

Internal module operation.

## `coop_navigation_sds/Configuration/scenarios.py`

Test-case and scenario configuration.

No public runtime components are defined in this module.

## `coop_navigation_sds/Configuration/schema.py`

Stable schemas, paths, and reproducibility metadata shared by all controllers.

### Function `safe_artifact_name(value, maximum_length=96)`

Return one portable, bounded filename component.

### Function `resolve_results_root(value=None)`

Return one stable results root independent of the process working directory.

### Class `RunArtifactPaths`

Canonical flat artifact paths for one completed experiment run.

- `RunArtifactPaths.as_dict(self)`: Internal class operation.

### Function `sanitized_config(config)`

Return a serializable configuration without persisted credentials.

### Function `runtime_environment_metadata()`

Capture lightweight reproducibility metadata without optional dependencies.

## `coop_navigation_sds/Configuration/settings.py`

Load and save script-friendly run settings as JSON.

### Function `default_settings_path()`

Return the configured settings file path.

### Function `load_run_settings(defaults=None, path=None)`

Merge saved JSON settings over supplied defaults.

### Function `save_run_settings(config, path=None)`

Atomically save persistent run settings and return their path.

## `coop_navigation_sds/Configuration/speech.py`

Agent B configuration and speech simulation constants.

### Function `_environment(name, legacy_name, default)`

Internal module operation.

### Function `_load_speech_pattern_presets()`

Load speech pattern presets from environment, falling back to defaults.

### Function `speech_pattern_keys()`

Return available configured speech pattern keys.

### Function `speech_pattern_settings(pattern_key)`

Return configured settings for a speech pattern key.

## `coop_navigation_sds/Configuration/travel.py`

Model-layer configuration.

### Function `_environment(name, legacy_name, default)`

Internal module operation.

## `coop_navigation_sds/DialogManagement/__init__.py`

Conversation state, policy orchestration, and full speech-pipeline execution.

No public runtime components are defined in this module.

## `coop_navigation_sds/DialogManagement/manager.py`

Main controller for a single dialog run.

### Function `route_reaches_goal(stations, scenario)`

Route reaches goal function for this module's MVC responsibility.

### Function `route_duration_min(stations, scenario, allowed_modes=None)`

Return total route duration in minutes when the route can be scheduled.

### Function `agent_b_model_name(agent_b_plugin)`

Return the model/plugin name used for Agent B metrics.

### Function `constraint_gap_missed(gap, transfer_tolerance=1)`

Return whether a valid proposal is worse than the stated constraint baseline.

### Function `agent_a_ended_conversation(text)`

Return whether Agent A has naturally closed the call.

### Function `agent_b_attempted_closure(text)`

Return whether Agent B tried to terminate instead of helping.

### Class `DialogManager`

Controller for one two-agent dialog.

- `DialogManager.__init__(self, test_case, agent_b_plugin, num_turns, route_interpreter=None, speech_transport=None, agent_a_responder=None, monitor=None, invalid_route_limit=2, constraint_miss_limit=2, stagnation_limit=2, transfer_tolerance=1, metric_snapshot_interval=1, max_turn_elapsed_sec=DEFAULT_MAX_TURN_ELAPSED_SEC, agent_a_objective_mode=None)`: init method for this module's MVC responsibility.
- `DialogManager.run(self, event_queue)`: Run method for this module's MVC responsibility.

## `coop_navigation_sds/DialogManagement/memory.py`

Route proposal memory and duplicate-detection helpers for one dialog.

### Class `RouteProposalMemory`

Tracks spoken route candidates so a dialog does not accept repeats.

- `RouteProposalMemory.__init__(self)`: Internal class operation.
- `RouteProposalMemory.key(self, route)`: Internal class operation.
- `RouteProposalMemory.already_seen(self, route)`: Internal class operation.
- `RouteProposalMemory.record(self, turn, route, duration, best_duration)`: Internal class operation.

## `coop_navigation_sds/DialogManagement/provider_runtime.py`

Persistent subprocess bridge for dependency-isolated speech providers.

### Function `_provider_key(engine)`

Internal module operation.

### Function `_valid_python(path)`

Internal module operation.

### Function `resolve_provider_python(engine, explicit='', environment_dir='.speech-providers')`

Resolve an explicit or manifest-managed provider interpreter.

### Class `ProviderProcessClient`

Maintain one JSON-lines worker so model weights remain loaded.

- `ProviderProcessClient.__init__(self, python, stage, engine, timeout_seconds, log_path=None)`: Internal class operation.
- `ProviderProcessClient._start(self)`: Internal class operation.
- `ProviderProcessClient._read_responses(self)`: Internal class operation.
- `ProviderProcessClient.request(self, payload)`: Internal class operation.
- `ProviderProcessClient.close(self)`: Internal class operation.
- `ProviderProcessClient.__del__(self)`: Internal class operation.

### Function `_worker_config(config, stage, engine)`

Internal module operation.

### Class `IsolatedTextToSpeech`

Text-to-speech proxy backed by a provider-specific Python runtime.

- `IsolatedTextToSpeech.__init__(self, engine, python, config)`: Internal class operation.
- `IsolatedTextToSpeech.synthesize(self, speaker, text)`: Internal class operation.

### Class `IsolatedSpeechToText`

Automatic speech recognition proxy backed by an isolated runtime.

- `IsolatedSpeechToText.__init__(self, engine, python, config)`: Internal class operation.
- `IsolatedSpeechToText.transcribe(self, signal)`: Internal class operation.

## `coop_navigation_sds/DialogManagement/provider_worker.py`

JSON-lines worker used by isolated speech-provider environments.

### Function `_write(response)`

Internal module operation.

### Function `main()`

Internal module operation.

## `coop_navigation_sds/DialogManagement/result.py`

Data model for the completed dialog result and a null event queue used by non-GUI experiment controllers.

### Class `DialogResult`

Data model containing the completed conversation, inferred route, route quality flags, timing, and extra run metadata.

### Class `NullEventQueue`

Controller helper that accepts events without displaying them for batch experiment runs.

- `NullEventQueue.put(self, event)`: Put method for this module's MVC responsibility.

## `coop_navigation_sds/DialogManagement/speech_pipeline.py`

Speech transport abstractions and simulated ASR/TTS transformations for dialog experiments.

### Function `synthesis_controls(prosody)`

Return only arguments accepted by a synthesis backend.

### Function `punctuation_pause_duration_sec(text, pause_ms)`

Return punctuation-weighted pause time for natural cadence.

### Function `_number_under_sixty(value)`

Internal module operation.

### Function `normalize_text_for_speech(text)`

Convert compact clock notation into recognizer-friendly spoken English.

### Class `SpeechPipelineError`

Raised when a strict speech pipeline stage fails.

- `SpeechPipelineError.__init__(self, message, diagnostics=None)`: Internal class operation.

### Class `SpeechEngineSpec`

Selectable speech backend metadata used by runtime and configuration UI.

### Function `available_tts_engine_keys()`

Return selectable text-to-speech implementations.

### Function `available_asr_engine_keys()`

Return selectable automatic speech recognition implementations.

### Function `speech_engine_description(kind, key)`

Return concise help for one configured speech backend.

### Function `resolve_espeak_executable(configured='')`

Internal module operation.

### Function `platform_default_tts_engine()`

Select an installed backend, with a dependency-free final fallback.

### Function `platform_default_asr_engine()`

Select an installed backend, with a dependency-free final fallback.

### Function `_prepare_optional_audio_runtime()`

Expose WinGet SoX installs and silence an irrelevant Qwen fallback banner.

### Function `_import_qwen_tts_model()`

Import Qwen TTS without printing its optional flash-attn fallback banner.

### Class `SpeechSignal`

Speech payload model passed between simulated TTS and ASR components.

### Class `SpeechPipelineConfig`

Runtime configuration for the mandatory speech stages.

- `SpeechPipelineConfig.applies_to(self, speaker: str)`: Internal class operation.
- `SpeechPipelineConfig.prosody_for(self, speaker: str)`: Return normalized synthesis controls for one agent.
- `SpeechPipelineConfig.label(self)`: Internal class operation.

### Class `SpeechPipelineTrace`

Trace of one generated utterance through optional speech stages.

- `SpeechPipelineTrace.signal(self)`: Internal class operation.

### Class `TextToSpeechEngine`

Protocol for TTS implementations that convert text into a speech signal.

- `TextToSpeechEngine.synthesize(self, speaker: str, text: str)`: Synthesize method for this module's MVC responsibility.

### Class `SpeechToTextEngine`

Protocol for ASR implementations that convert speech signals into transcript text.

- `SpeechToTextEngine.transcribe(self, signal: SpeechSignal)`: Transcribe method for this module's MVC responsibility.

### Class `WaveFileTextToSpeech`

Dependency-free TTS adapter that writes a simple WAV carrier plus transcript sidecar.

- `WaveFileTextToSpeech.__init__(self, audio_dir=SPEECH_AUDIO_DIR, sample_rate=8000, playback_enabled=False, realtime_enabled=False, agent_a_words_per_minute=140, agent_b_words_per_minute=145, max_duration_sec=20.0, pattern_key='clean', config=None)`: Internal class operation.
- `WaveFileTextToSpeech.synthesize(self, speaker: str, text: str)`: Internal class operation.
- `WaveFileTextToSpeech._write_wave(self, path: Path, speaker: str, text: str)`: Internal class operation.
- `WaveFileTextToSpeech._play_wave(path: Path, realtime=False, fallback_duration=0.0)`: Internal class operation.

### Class `WaveFileSpeechToText`

ASR adapter for generated speech artifacts using their transcript sidecar.

- `WaveFileSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.

### Function `_write_float_wave(path, samples, sample_rate)`

Write mono floating-point samples as a 16-bit PCM WAV.

### Function `_wave_duration(path)`

Internal module operation.

### Class `OptionalTextToSpeechBase`

Common artifact, playback, and failure handling for optional TTS engines.

- `OptionalTextToSpeechBase.__init__(self, config)`: Internal class operation.
- `OptionalTextToSpeechBase.synthesize(self, speaker: str, text: str)`: Internal class operation.
- `OptionalTextToSpeechBase._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `OptionalTextToSpeechBase.install_hint(self)`: Internal class operation.

### Class `ChatTTSTextToSpeech`

Lazy ChatTTS adapter using the official Python API.

- `ChatTTSTextToSpeech.__init__(self, config)`: Internal class operation.
- `ChatTTSTextToSpeech._model(self)`: Internal class operation.
- `ChatTTSTextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `ChatTTSTextToSpeech._speaker_embedding(self, chat, torch, seed)`: Return a cached speaker tensor without ChatTTS' memory-heavy LZMA encoding.
- `ChatTTSTextToSpeech.install_hint(self)`: Internal class operation.
- `ChatTTSTextToSpeech._import_chattts(self)`: Internal class operation.
- `ChatTTSTextToSpeech._ensure_base16384()`: Prefer the native codec and supply the ChatTTS subset if its wheel is incomplete.

### Class `MeloTextToSpeech`

MeloTTS adapter using its documented command-line interface.

- `MeloTextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `MeloTextToSpeech.install_hint(self)`: Internal class operation.

### Class `PiperTextToSpeech`

Piper adapter using the official Python API and an explicit ONNX voice.

- `PiperTextToSpeech.__init__(self, config)`: Internal class operation.
- `PiperTextToSpeech._model(self)`: Internal class operation.
- `PiperTextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `PiperTextToSpeech.install_hint(self)`: Internal class operation.

### Class `EspeakNgTextToSpeech`

Cross-platform eSpeak NG command-line synthesis adapter.

- `EspeakNgTextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `EspeakNgTextToSpeech.install_hint(self)`: Internal class operation.

### Class `KokoroTextToSpeech`

Kokoro adapter using its reusable Python pipeline.

- `KokoroTextToSpeech.__init__(self, config)`: Internal class operation.
- `KokoroTextToSpeech._language_code(language)`: Internal class operation.
- `KokoroTextToSpeech._pipeline(self, language)`: Internal class operation.
- `KokoroTextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `KokoroTextToSpeech.install_hint(self)`: Internal class operation.

### Class `F5TextToSpeech`

F5-TTS adapter using the official inference command.

- `F5TextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `F5TextToSpeech.install_hint(self)`: Internal class operation.

### Class `Qwen3TextToSpeech`

Qwen3-TTS adapter for custom speakers or reference-voice cloning.

- `Qwen3TextToSpeech.__init__(self, config)`: Internal class operation.
- `Qwen3TextToSpeech._isolated_runtime(self)`: Internal class operation.
- `Qwen3TextToSpeech._worker_request(self, payload)`: Internal class operation.
- `Qwen3TextToSpeech._model(self, model_name)`: Internal class operation.
- `Qwen3TextToSpeech._language(language)`: Internal class operation.
- `Qwen3TextToSpeech._synthesize_wave(self, wav_path, text, prosody)`: Internal class operation.
- `Qwen3TextToSpeech.install_hint(self)`: Internal class operation.

### Class `WindowsSapiTextToSpeech`

Windows SAPI TTS stage that writes actual spoken WAV audio.

- `WindowsSapiTextToSpeech.__init__(self, audio_dir=SPEECH_AUDIO_DIR, playback_enabled=False, realtime_enabled=False, voice_rate=-3, pattern_key='clean', config=None)`: Internal class operation.
- `WindowsSapiTextToSpeech.synthesize(self, speaker: str, text: str)`: Internal class operation.
- `WindowsSapiTextToSpeech._powershell_command(wav_path: Path, voice='', speech_rate=-3, words_per_minute=140, volume=100, pitch_semitones=0, pause_ms=260, emphasis='none')`: Internal class operation.
- `WindowsSapiTextToSpeech._wave_duration(path: Path)`: Internal class operation.

### Class `WindowsSapiSpeechToText`

Windows SAPI ASR stage that transcribes a WAV file.

- `WindowsSapiSpeechToText.__init__(self, language='en-US', phrase_hints=None, initial_silence_sec=4.0, babble_timeout_sec=6.0, end_silence_ms=2500, ambiguous_end_silence_ms=4500)`: Internal class operation.
- `WindowsSapiSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.
- `WindowsSapiSpeechToText._powershell_command(wav_path: Path, language='en-US', initial_silence_sec=4.0, babble_timeout_sec=6.0, end_silence_ms=2500, ambiguous_end_silence_ms=4500)`: Internal class operation.
- `WindowsSapiSpeechToText._parse_result(raw_output)`: Internal class operation.
- `WindowsSapiSpeechToText._best_domain_alternative(self, transcript, alternatives)`: Internal class operation.
- `WindowsSapiSpeechToText._normalize_domain_terms(self, transcript)`: Repair high-confidence spelling variants using only the public domain lexicon.
- `WindowsSapiSpeechToText._default_phrase_hints()`: Internal class operation.

### Class `FasterWhisperSpeechToText`

Optional local Faster-Whisper ASR adapter.

- `FasterWhisperSpeechToText.__init__(self, model_name='small.en', device='auto', compute_type='default', language='en', beam_size=5, min_silence_duration_ms=1200, model_cache_dir='.speech-providers/models/faster-whisper')`: Internal class operation.
- `FasterWhisperSpeechToText._load_model(self)`: Internal class operation.
- `FasterWhisperSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.

### Function `_speech_audio_path(signal, engine_name)`

Return the readable WAV path carried by a speech signal.

### Function `_record_asr_diagnostics(signal, engine_name, transcript, **details)`

Internal module operation.

### Class `VoskSpeechToText`

Offline Vosk recognizer for mono PCM WAV artifacts.

- `VoskSpeechToText.__init__(self, model_name='', language='en-US', search_width=1, model_cache_dir='.speech-providers/models/vosk')`: Internal class operation.
- `VoskSpeechToText._load_model(self)`: Internal class operation.
- `VoskSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.
- `VoskSpeechToText._result_text(payload)`: Read Vosk's ordinary or N-best result structure.

### Class `SherpaOnnxSpeechToText`

Offline sherpa-onnx adapter with model-directory auto-detection.

- `SherpaOnnxSpeechToText.__init__(self, model_name='', language='en-US', search_width=1)`: Internal class operation.
- `SherpaOnnxSpeechToText._first(root, patterns)`: Internal class operation.
- `SherpaOnnxSpeechToText._load_model(self)`: Internal class operation.
- `SherpaOnnxSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.
- `SherpaOnnxSpeechToText.install_hint()`: Internal class operation.

### Class `WhisperCppSpeechToText`

Portable whisper.cpp adapter using the official whisper-cli executable.

- `WhisperCppSpeechToText.__init__(self, model_name='', executable='', language='en', vad_model='', search_width=1, timeout_sec=60.0, provider_environment_dir='.speech-providers')`: Internal class operation.
- `WhisperCppSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.

### Class `ParakeetSpeechToText`

NVIDIA NeMo adapter for Parakeet automatic speech recognition.

- `ParakeetSpeechToText.__init__(self, model_name='', device='auto')`: Internal class operation.
- `ParakeetSpeechToText._load_model(self)`: Internal class operation.
- `ParakeetSpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.

### Class `Qwen3SpeechToText`

Qwen3-ASR adapter using the official Python package.

- `Qwen3SpeechToText.__init__(self, model_name='', device='auto', language='en-US', model_cache_dir='.speech-providers/models/qwen3-asr')`: Internal class operation.
- `Qwen3SpeechToText._load_model(self)`: Internal class operation.
- `Qwen3SpeechToText.transcribe(self, signal: SpeechSignal)`: Internal class operation.

### Function `_persona_random(speaker, text, prosody, stage)`

Internal module operation.

### Function `_apply_persona_delivery(text, speaker, prosody)`

Apply reproducible lexical manifestations of the selected delivery style.

### Function `_apply_persona_recognition_errors(text, speaker, prosody)`

Apply logged deterministic transcript degradation for controlled personas.

### Class `SpeechTransport`

Controller utility that composes TTS and ASR engines into one transmit operation.

- `SpeechTransport.__init__(self, tts_engine=None, asr_engine=None, config=None)`: init method for this module's MVC responsibility.
- `SpeechTransport._default_tts_engine(self)`: Internal class operation.
- `SpeechTransport._default_asr_engine(self)`: Internal class operation.
- `SpeechTransport._stage_engine(self, stage_engine)`: Internal class operation.
- `SpeechTransport._isolated_stage(self, stage, engine)`: Use another Python 3.14 interpreter only when explicitly configured.
- `SpeechTransport.description(self)`: Description method for this module's MVC responsibility.
- `SpeechTransport.validate_configuration(self)`: Fail early unless both stages are actual configured components.
- `SpeechTransport.close(self)`: Release provider workers and their operating-system resources.
- `SpeechTransport.health_check(self)`: Run a short end-to-end check through both agents before a speech dialog.
- `SpeechTransport.transmit(self, speaker: str, text: str)`: Transmit method for this module's MVC responsibility.
- `SpeechTransport.transmit_trace(self, speaker: str, text: str)`: Run mandatory TTS and ASR stages and return a full trace.
- `SpeechTransport.estimate_duration_sec(self, speaker: str, text: str)`: Estimate natural speech duration from utterance length and speaker rate.

### Class `PatternedSpeechToText`

Configurable ASR simulator that injects configured transcript patterns.

- `PatternedSpeechToText.__init__(self, pattern_key='clean', seed=0)`: init method for this module's MVC responsibility.
- `PatternedSpeechToText.transcribe(self, signal: SpeechSignal)`: Transcribe method for this module's MVC responsibility.
- `PatternedSpeechToText.transform_text(self, text: str, include_recognition_errors=True)`: Apply the configured speech-pattern transformations.
- `PatternedSpeechToText._insert_tokens(self, text: str, probability: float, tokens, sentence_start=False)`: Insert configured filler, pause, or hesitation tokens.
- `PatternedSpeechToText._add_stutters(self, text: str, probability: float, max_words=1)`: Internal class operation.
- `PatternedSpeechToText._compress(self, text: str)`: compress method for this module's MVC responsibility.
- `PatternedSpeechToText._substitute_words(self, text: str, probability: float, substitutions)`: Internal class operation.
- `PatternedSpeechToText._drop_some_words(self, text: str, drop_probability: float, protected_terms=())`: drop some words method for this module's MVC responsibility.

## `coop_navigation_sds/DialogManagement/stages.py`

Conversation-stage and context inference shared by both dialog agents.

### Class `ConversationStage`

Observable stages of one route-planning conversation.

- `ConversationStage.label(self)`: Internal class operation.

### Class `DialogContext`

Compact conversational memory derived from the heard transcript.

### Class `AgentMemoryView`

One agent's persistent view of intended speech and heard transcripts.

- `AgentMemoryView.prompt_summary(self)`: Return a compact grounding reminder for a language-model prompt.

### Function `_latest(conversation, speaker)`

Internal module operation.

### Function `_format_clock(minutes)`

Internal module operation.

### Function `_task_variable_summary(task_variables, missing)`

Internal module operation.

### Function `_task_variables_for(owner, conversation, scenario=None)`

Return critical task variables from the selected agent's own perspective.

### Function `_constraint_detail(key, scenario=None, persona=None)`

Internal module operation.

### Function `_response_focus(text)`

Internal module operation.

### Function `dialog_context(conversation)`

Infer the current stage and latest conversational focus.

### Function `_current_route_memory(conversation, scenario=None, persona=None)`

Return the latest route candidate visible in this transcript.

### Function `agent_memory_view(owner, conversation, scenario=None, persona=None)`

Build a memory view from one agent's perspective-specific history.

## `coop_navigation_sds/DialogManagement/whisper_cpp_runtime.py`

whisper.cpp runtime discovery shared by preflight and live ASR.

### Function `_provider_root(environment_dir)`

Internal module operation.

### Function `_existing_file(value, base=None)`

Internal module operation.

### Function `_valid_executable_path(value)`

Internal module operation.

### Function `_manifest_entry(environment_dir)`

Internal module operation.

### Function `_path_candidates(root, names)`

Internal module operation.

### Function `_model_candidates(root)`

Internal module operation.

### Function `resolve_whisper_cpp_paths(executable='', model='', vad_model='', environment_dir='.speech-providers')`

Resolve whisper.cpp executable and model paths from config, manifest, or provider folder.

### Function `whisper_cpp_ready(executable='', model='', vad_model='', environment_dir='.speech-providers')`

Return readiness and human-readable diagnostics for whisper.cpp.

## `coop_navigation_sds/EvaluationMetrics/__init__.py`

Retrospective phase, task, speech-quality, and validity metrics.

No public runtime components are defined in this module.

## `coop_navigation_sds/EvaluationMetrics/catalog.py`

Canonical selectable metric catalog in dialogue-system execution order.

### Function `phase_key(family)`

Return the explicit stable phase identifier for a catalog family.

### Function `metric_local_name(metric_key)`

Return a readable phase-local metric name while retaining its global key.

### Function `global_metric_key(phase, local_name)`

Resolve a phase-local exported name back to the stable global metric key.

### Function `metric_metadata(key, phase, metric_tiers=None)`

Return manifest metadata for one catalog metric.

### Function `metric_calculation_method(key)`

Return the concise calculation shown in console and research manifests.

### Function `metric_scale_percentage(key, value)`

Return percentage of a bounded metric's documented maximum.

## `coop_navigation_sds/EvaluationMetrics/dnsmos.py`

Retrospective DNSMOS evaluation for generated speech artifacts.

### Class `DNSMOSEvaluator`

Evaluate WAV artifacts with TorchMetrics' DNSMOS implementation.

- `DNSMOSEvaluator.__init__(self, personalized=False, device='cpu', num_threads=None)`: Internal class operation.
- `DNSMOSEvaluator.evaluate(self, items)`: Internal class operation.

## `coop_navigation_sds/EvaluationMetrics/metrics.py`

Automatic evaluation metrics for speech-dialog route-planning experiments.

### Function `metric_tier_config_with_defaults(config=None)`

Return a legacy-compatible metric label map.

### Function `obligatory_metric_map()`

Return the complete obligatory retrospective metric set.

### Function `enabled_metric_family_specs(config=None, tiers=None)`

Return every canonical metric family.

### Function `phase_key_from_title(title)`

Return the explicit phase key, with a compatibility fallback for callers.

### Class `MetricRecord`

Serializable scalar metrics for one completed dialog run.

- `MetricRecord.as_dict(self)`: Internal class operation.

### Function `safe_ratio(numerator, denominator)`

Internal module operation.

### Function `tokenize_words(text)`

Internal module operation.

### Function `normalized_text(text)`

Internal module operation.

### Function `station_mentions_in_text(text)`

Internal module operation.

### Function `ordered_station_mentions(text)`

Return station mentions in the order they appear in text.

### Function `delexicalize_text(text)`

Replace station and numeric mentions with placeholders for coarse semantic scoring.

### Function `ngram_counts(tokens, n)`

Internal module operation.

### Function `simple_bleu(reference_tokens, hypothesis_tokens, max_n=2)`

Internal module operation.

### Function `simple_rouge(reference_tokens, hypothesis_tokens)`

Internal module operation.

### Function `simple_meteor(reference_tokens, hypothesis_tokens)`

Internal module operation.

### Function `token_jaccard(reference_tokens, hypothesis_tokens)`

Internal module operation.

### Function `simple_rouge_l(reference_tokens, hypothesis_tokens)`

Internal module operation.

### Function `f1_score(precision, recall)`

Internal module operation.

### Function `edit_counts(reference_tokens, hypothesis_tokens)`

Internal module operation.

### Function `sequence_error_rate(reference, hypothesis)`

Return edit distance normalized by the reference sequence length.

### Function `preservation_rate(reference_items, hypothesis_items)`

Return multiset recall for values that must survive a transformation.

### Function `numeric_mentions(text)`

Internal module operation.

### Function `negation_mentions(text)`

Internal module operation.

### Function `constraint_mentions(text)`

Internal module operation.

### Function `analyze_wave_file(path)`

Return inexpensive deterministic acoustic measurements for a PCM WAV.

### Function `mean_or_none(values)`

Internal module operation.

### Function `variance_or_none(values)`

Internal module operation.

### Function `pearson_correlation(left, right)`

Internal module operation.

### Function `wilson_interval(successes, total, z=1.96)`

Return a Wilson score interval for a binomial proportion.

### Class `MetricComputer`

Compute thesis-facing automatic evaluation metrics from a completed dialog.

- `MetricComputer.__init__(self, nisqa_evaluator=None, dnsmos_evaluator=None, reference_audio_evaluator=None)`: Internal class operation.
- `MetricComputer.compute(self, result, scenario)`: Internal class operation.

### Function `_record_group_key(record)`

Return the condition identity without the iteration suffix.

### Function `_group_success_gap(records, attribute)`

Internal module operation.

### Function `_average_metric_outcome_correlation(records)`

Internal module operation.

### Function `failure_indicator_analysis(records, minimum_per_outcome=3)`

Find exploratory pre-outcome thresholds associated with failed runs.

### Function `_maximum_metric_redundancy(records)`

Internal module operation.

### Function `apply_cross_run_metrics(records)`

Populate metrics that require a completed multi-run experiment.

### Function `apply_paired_run_metrics(records)`

Attach audio-minus-text deltas to matched experimental controls.

## `coop_navigation_sds/EvaluationMetrics/nisqa.py`

Retrospective NISQA v2 evaluation for generated speech artifacts.

### Function `read_mono_pcm_wave(path)`

Read a PCM WAV as mono floating-point samples without audio dependencies.

### Class `NISQAEvaluator`

Evaluate WAV artifacts with TorchMetrics' NISQA v2 implementation.

- `NISQAEvaluator.evaluate(self, items)`: Internal class operation.

## `coop_navigation_sds/EvaluationMetrics/reference_audio.py`

Retrospective intrusive speech-quality metrics for aligned audio pairs.

### Function `_aligned_pair(reference_path, degraded_path)`

Internal module operation.

### Function `_si_sdr(reference, degraded)`

Internal module operation.

### Class `ReferenceAudioQualityEvaluator`

Evaluate PESQ, STOI, SI-SDR, and externally supplied licensed POLQA.

- `ReferenceAudioQualityEvaluator.evaluate(self, items)`: Internal class operation.

## `coop_navigation_sds/experiments.py`

Batch experiment controller that executes condition grids and serializes metric records.

### Class `DropQueue`

Queue adapter that discards human-readable batch events.

- `DropQueue.put(self, _event)`: Internal class operation.

### Class `ExperimentCondition`

Data model for one batch experiment condition.

- `ExperimentCondition.__post_init__(self)`: Internal class operation.

### Class `ExperimentRunner`

Batch controller for running one condition or a full condition grid.

- `ExperimentRunner.__init__(self, model_adapter, num_turns, agent_b_plugin_key=AGENT_B_PLUGIN, tts_engine=SPEECH_TTS_ENGINE, asr_engine=SPEECH_ASR_ENGINE, speech_audio_dir=SPEECH_AUDIO_DIR, speech_playback_enabled=SPEECH_PLAYBACK_ENABLED, speech_realtime_enabled=SPEECH_REALTIME_ENABLED, speech_synthesis_config=None, transfer_tolerance=AGENT_A_TRANSFER_TOLERANCE, invalid_route_limit=INVALID_ROUTE_LIMIT, constraint_miss_limit=CONSTRAINT_MISS_LIMIT, stagnation_limit=2, max_turn_elapsed_sec=DEFAULT_MAX_TURN_ELAPSED_SEC, calculation_max_time_sec=GENERATION_MAX_TIME_SEC, llm_agent_a=LLM_AGENT_A, agent_a_type=None, log_profile=LOG_PROFILE_OFF, log_dir=SESSION_LOG_DIR, scenario_overrides=None, model_adapter_factory=None, agent_a_model_adapter=None, experiment_specification=None)`: init method for this module's MVC responsibility.
- `ExperimentRunner.run_condition(self, condition: ExperimentCondition, *, compute_metrics=True)`: Run condition method for this module's MVC responsibility.
- `ExperimentRunner._event_queue_for(self, condition: ExperimentCondition)`: Return a no-op queue by default, or a structured logger for batch audits.
- `ExperimentRunner._configure_model_adapter_runtime(self, model_adapter, calculation_max_time_sec=None)`: Internal class operation.
- `ExperimentRunner._agent_a_responder_for(self, model_adapter)`: Internal class operation.
- `ExperimentRunner._model_adapter_for(self, condition: ExperimentCondition)`: model adapter for method for this module's MVC responsibility.
- `ExperimentRunner.run_grid(self, conditions, collect_results=True)`: Run grid method for this module's MVC responsibility.

### Function `pairwise_factor_rows(factors)`

Return deterministic strength-two rows covering every pair of factor levels.

### Function `condition_coverage_report(conditions)`

Summarize value and pair coverage from expanded audio conditions.

### Function `build_condition_grid(test_case_keys=None, persona_keys=None, speech_pattern_keys=None, model_param_keys=None, objective_modes=None, agent_a_audio_persona_keys=None, agent_b_audio_persona_keys=None, tts_engine_keys=None, asr_engine_keys=None, agent_b_model_keys=None, iterations=1, parameter_grid=None, parameter_profiles=None, linked_profiles=None, coverage_strategy='full_factorial', pair_audio_with_text=False)`

Build condition grid function for this module's MVC responsibility.

## `coop_navigation_sds/NaturalLanguageGeneration/__init__.py`

Agent utterance generation, policies, prompts, and language-model adapters.

No public runtime components are defined in this module.

## `coop_navigation_sds/NaturalLanguageGeneration/assistant/__init__.py`

Agent B package.

No public runtime components are defined in this module.

## `coop_navigation_sds/NaturalLanguageGeneration/assistant/pipeline.py`

Agent B controller pipeline.

### Function `_clock_token_value(token)`

Internal module operation.

### Function `_time_context(text, latest_agent_b_question='')`

Internal module operation.

### Function `_short_clock_answer(text)`

Internal module operation.

### Function `parse_heard_clock(text, allow_contextless=True)`

Parse numeric or naturally spoken 24-hour clock expressions.

### Function `heard_trip_report(conversation)`

Recover trip slots and evidence from Agent B's accumulated heard memory.

### Function `heard_trip_facts(conversation)`

Recover trip slots from Agent B's accumulated ASR-grounded memory.

### Function `heard_constraint_report(conversation)`

Extract value-bearing private constraints only from Agent A transcripts.

### Class `DialogState`

Small state model passed between dialog controller stages for a single turn.

- `DialogState.scenario(self)`: Scenario method for this module's MVC responsibility.
- `DialogState.persona(self)`: Persona method for this module's MVC responsibility.
- `DialogState.context(self)`: Return conversational memory derived only from heard utterances.
- `DialogState.memory(self)`: Return Agent B's persistent intended/heard memory view.
- `DialogState.stage(self)`: Return the current explicit conversation stage.
- `DialogState.assistant_scenario(self)`: Return journey facts Agent B actually received through ASR.
- `DialogState.heard_constraint_state(self)`: Return private constraint values recoverable from Agent B's transcript.
- `DialogState.recognized_constraint_keys(self)`: Return stated constraints whose required values were actually understood.
- `DialogState.missing_trip_slots(self)`: Return trip facts still absent from Agent B's heard memory.
- `DialogState.heard_trip_state(self)`: Return fact recovery state used by Agent B before planning.
- `DialogState.trip_clarification_prompt(self)`: Return the shared repair prompt for every Agent B backend.
- `DialogState.latest_agent_a_text(self)`: Return the latest heard caller utterance.

### Class `VerbalTransformationPipeline`

Transforms dialog state into a verbal Agent B response.

- `VerbalTransformationPipeline.__init__(self, model_adapter)`: init method for this module's MVC responsibility.
- `VerbalTransformationPipeline.run_agent_b(self, state: DialogState)`: Run agent b method for this module's MVC responsibility.
- `VerbalTransformationPipeline._generate_with_audit(self, state, messages, purpose)`: Internal class operation.
- `VerbalTransformationPipeline._reply_decision(self, reply, state, scenario)`: Internal class operation.
- `VerbalTransformationPipeline.consume_prompt_audits(self)`: Return exact prompt calls made since the previous controller read.
- `VerbalTransformationPipeline.reply_reaches_goal(self, reply, scenario)`: Internal class operation.

### Class `AgentBPromptStage`

Class component; see its typed members and call sites.

- `AgentBPromptStage.run(self, state: DialogState)`: Internal class operation.
- `AgentBPromptStage.run_repair(self, state: DialogState, repeated_reply: str)`: Internal class operation.
- `AgentBPromptStage._build_messages(self, state, repair)`: Internal class operation.
- `AgentBPromptStage._system_prompt(state, repair)`: Internal class operation.
- `AgentBPromptStage.phase_fallback(self, state: DialogState)`: Internal class operation.

### Class `ModelGenerationStage`

Class component; see its typed members and call sites.

- `ModelGenerationStage.__init__(self, model_adapter)`: Internal class operation.
- `ModelGenerationStage.run(self, messages)`: Internal class operation.

### Function `build_prompt_from_messages(messages)`

Fallback serializer for legacy deterministic adapters.

### Class `VerbalCleanupStage`

Class component; see its typed members and call sites.

- `VerbalCleanupStage.run(self, raw_reply: str)`: Internal class operation.
- `VerbalCleanupStage.repeats_prior_agent_b(self, reply: str, conversation)`: Internal class operation.
- `VerbalCleanupStage._normalize(self, text: str)`: Internal class operation.

## `coop_navigation_sds/NaturalLanguageGeneration/assistant/plugin_registry.py`

Agent B plugin configuration and loading.

### Class `AgentBModelSpec`

Public metadata for a selectable Agent B implementation.

### Function `agent_b_plugin_description(plugin_key)`

Return concise UI help for a built-in or custom Agent B implementation.

### Class `AgentBPluginConfig`

Runtime selection for Agent B.

- `AgentBPluginConfig.from_env(cls)`: Internal class operation.
- `AgentBPluginConfig.normalized_key(self)`: Internal class operation.
- `AgentBPluginConfig.needs_model(self)`: Internal class operation.
- `AgentBPluginConfig.label(self)`: Internal class operation.

### Class `LlmAgentBPlugin`

Default CoopNavigationSDS Agent B backed by a configured model adapter.

- `LlmAgentBPlugin.__init__(self, model_adapter)`: Internal class operation.
- `LlmAgentBPlugin.run_agent_b(self, state)`: Internal class operation.
- `LlmAgentBPlugin.consume_prompt_audits(self)`: Expose prompt evidence without coupling orchestration to pipeline internals.

### Class `SimplePlannerAgentBPlugin`

Small deterministic Agent B plugin for demos and tests.

- `SimplePlannerAgentBPlugin.run_agent_b(self, state)`: Internal class operation.

### Class `ResearchPlannerAgentBPlugin`

Deterministic research policy with an explicit route-selection strategy.

- `ResearchPlannerAgentBPlugin.run_agent_b(self, state)`: Internal class operation.
- `ResearchPlannerAgentBPlugin.select_option(self, options, prior_routes)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._risk_value(value)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._vector(cls, option)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._dominates(cls, left, right)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._pareto_tiebreak(cls, option)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._robust_key(cls, option)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._route_edges(route)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._diverse_key(cls, option, prior_routes)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._prior_routes(conversation, scenario)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._format_option(self, option, context)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._format_existing_option(self, option, context)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._requested_detail(option, context)`: Internal class operation.
- `ResearchPlannerAgentBPlugin._style_prefix(self, context, existing=False)`: Internal class operation.

### Class `ParetoPlannerAgentBPlugin`

Class component; see its typed members and call sites.

### Class `RobustPlannerAgentBPlugin`

Class component; see its typed members and call sites.

### Class `DiversePlannerAgentBPlugin`

Class component; see its typed members and call sites.

### Class `FunctionAgentBPlugin`

Adapter for function plugins.

- `FunctionAgentBPlugin.__init__(self, fn)`: Internal class operation.
- `FunctionAgentBPlugin.run_agent_b(self, state)`: Internal class operation.

### Function `available_agent_b_plugin_keys(extra_key=None)`

Internal module operation.

### Function `create_agent_b_plugin(plugin_key, model_adapter)`

Create an Agent B plugin from a built-in key or `module:factory` path.

### Function `load_custom_agent_b_plugin(plugin_path, model_adapter)`

Load `module:factory_or_class` and return a run_agent_b-compatible object.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/__init__.py`

Agent A package.

No public runtime components are defined in this module.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/agents.py`

Prompt and utterance helpers for Agent A and Agent B.

### Function `initial_conversation(scenario)`

Initial conversation function for this module's MVC responsibility.

### Function `build_prompt(active_agent_name, active_agent_system, history)`

Internal module operation.

### Function `build_messages(active_agent_name, active_agent_system, history)`

Internal module operation.

### Function `clean_reply(text)`

Internal module operation.

### Function `fallback_reply(active_agent_name, scenario, route_index=0, persona=None, conversation=None)`

Internal module operation.

### Function `agent_a_requested_secondary_constraints(conversation)`

Return whether Agent A has asked about non-time route constraints.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/config.py`

Agent A configuration and prompt constants.

No public runtime components are defined in this module.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/personas.py`

Persona model definitions for Agent A behavior and standardized experiment conditions.

### Function `get_persona(persona_key: str)`

Get persona function for this module's MVC responsibility.

### Function `preference_text(persona: dict)`

Format a persona preference profile for prompts.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/prompt_data.py`

Prompt-context helpers that describe the current transit model and task rules to the language models.

### Function `network_vocabulary_text()`

Return the shared station and line vocabulary known by both agents.

### Function `compact_prompt_context(scenario, persona=None, stated_constraint_keys=())`

Return Agent B's verified candidates using only constraints heard so far.

### Function `caller_prompt_context(scenario, persona=None)`

Return only the information a hotline caller plausibly knows.

### Function `compact_route_candidate_text(scenario, persona=None, stated_constraint_keys=(), limit=3)`

Return concise valid route candidates instead of the full network.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/prompting.py`

Shared prompt policy for dialog agents.

### Function `agent_a_repeat_trip_facts(scenario, conversation=None)`

Return a concise caller repetition after Agent B did not understand.

### Function `agent_a_word_correction(question, scenario, conversation=None)`

Answer a word-level clarification with one known term only.

### Function `agent_a_requested_trip_fact(question, scenario)`

Answer one explicitly requested missing trip slot without extra detail.

### Function `agent_a_unclear_route_reply(text, destination)`

Return a compact repair request for a nonsensical Agent B answer.

### Function `build_agent_a_system(persona, scenario)`

Internal module operation.

### Function `build_agent_b_system(scenario, persona=None, stated_constraint_keys=())`

Internal module operation.

### Function `build_agent_b_phase_instruction(turn, destination)`

Compatibility wrapper for callers that still provide a turn index.

### Function `build_agent_b_stage_instruction(stage, destination)`

Return concise Agent B guidance for an explicit conversation stage.

### Function `generate_agent_a_template(turn, persona, scenario, conversation=None)`

Internal module operation.

### Function `progressive_constraint_limit(persona, scenario)`

Return the configured number of private constraints Agent A may reveal.

### Function `constraint_sequence_complete(persona, scenario, stated_keys)`

Return whether every configured progressive constraint has been stated.

### Function `route_comparison_complete(conversation, scenario, allowed_modes)`

Return whether enough distinct valid routes have been discussed.

### Function `final_agent_a_turn(scenario, conversation)`

Return whether the next Agent A reply consumes the configured turn budget.

### Function `agent_a_route_reaction(turn, persona, scenario, conversation)`

Return a concise caller reaction based on Agent B's latest route reply.

### Function `agent_a_alternative_request(persona)`

Choose the next route-improvement request from persona constraints.

### Function `agent_a_secondary_constraint_request(persona)`

Return non-time constraints Agent A should mention after valid routes exist.

### Function `secondary_constraints_already_asked(conversation)`

Internal module operation.

### Function `valid_agent_b_route_count(conversation, scenario, allowed_modes)`

Count distinct valid Agent B route proposals in the conversation.

### Function `best_prior_route_duration(conversation, scenario)`

Return the best earlier Agent B route duration, if any.

### Function `best_prior_route_matching_constraints(conversation, scenario, persona, stated_keys, duration_limit, constraint_route=None)`

Return the best prior Agent B route satisfying the current revealed goals.

## `coop_navigation_sds/NaturalLanguageGeneration/caller/responder.py`

Agent A controller strategies.

### Function `normalize_agent_a_type(value=None, legacy_llm_agent_a=False)`

Normalize the caller implementation while preserving the legacy boolean.

### Function `available_agent_a_types()`

Internal module operation.

### Function `agent_a_uses_model(agent_a_type)`

Return whether the Agent A implementation requires a language model.

### Class `TemplateAgentAResponder`

Fast deterministic Agent A behavior for repeatable experiments.

- `TemplateAgentAResponder.reply(self, turn: int, persona: dict, scenario: dict, conversation: list)`: Reply method for this module's MVC responsibility.

### Class `LLMAgentAResponder`

Agent A behavior backed by the same ModelAdapter interface as Agent B.

- `LLMAgentAResponder.__init__(self, model_adapter)`: init method for this module's MVC responsibility.
- `LLMAgentAResponder.reply(self, turn: int, persona: dict, scenario: dict, conversation: list)`: Reply method for this module's MVC responsibility.
- `LLMAgentAResponder.consume_prompt_audits(self)`: Return exact prompt calls made since the previous controller read.

### Function `_closes_call(text)`

Internal module operation.

## `coop_navigation_sds/NaturalLanguageGeneration/model_runtime.py`

Runtime loader and provider-neutral model-adapter factory.

### Function `_model_cache_dir()`

Internal module operation.

### Function `_prepared_model(model_name)`

Internal module operation.

### Function `load_model_and_tokenizer(model_name: str=MODEL, token: str | None=TOKEN, device: str=DEVICE, allow_model_download: bool=ALLOW_MODEL_DOWNLOAD)`

Load model and tokenizer function for this module's MVC responsibility.

### Function `create_transformers_adapter(model_name: str=MODEL, token: str | None=TOKEN, device: str=DEVICE, max_new_tokens: int=MAX_NEW_TOKENS, max_input_tokens: int=MAX_INPUT_TOKENS, allow_model_download: bool=ALLOW_MODEL_DOWNLOAD)`

Create transformers adapter function for this module's MVC responsibility.

### Function `create_model_adapter(provider: str=MODEL_PROVIDER, *, model_name: str | None=None, api_key: str | None=None, base_url: str | None=None, timeout_sec: float=CHAT_TIMEOUT_SEC, device: str=DEVICE, max_new_tokens: int=MAX_NEW_TOKENS, max_input_tokens: int=MAX_INPUT_TOKENS, allow_model_download: bool=ALLOW_MODEL_DOWNLOAD)`

Create the configured provider-neutral model adapter.

### Function `_load_tokenizer(model_name: str, token: str | None, allow_model_download: bool=ALLOW_MODEL_DOWNLOAD)`

load tokenizer function for this module's MVC responsibility.

### Function `_load_model(model_name: str, token: str | None, device: str, allow_model_download: bool=ALLOW_MODEL_DOWNLOAD)`

load model function for this module's MVC responsibility.

## `coop_navigation_sds/NaturalLanguageGeneration/models.py`

Model backend adapter interfaces and Hugging Face transformer implementation used by dialog controllers.

### Class `ChatMessage`

Provider-neutral chat message.

### Function `_ollama_model_catalog(base_url, timeout_sec=5.0)`

Internal module operation.

### Function `_ollama_model_names(base_url, timeout_sec=5.0)`

Internal module operation.

### Function `_warm_ollama_model(base_url, model, timeout_sec)`

Internal module operation.

### Function `ollama_executable()`

Return the installed Ollama command path for Windows or Linux.

### Function `ollama_model_inventory(base_url, *, autostart=True, timeout_sec=10.0, models_dir=None)`

Return one reachable Ollama service and its installed model names.

### Function `ensure_ollama_models_ready(base_url, models, *, autostart=True, timeout_sec=10.0, warm_models=False, models_dir=None)`

Verify every requested Ollama model before an experiment starts.

### Function `ensure_ollama_ready(base_url, model, *, autostart=True, timeout_sec=10.0, warm_model=False, models_dir=None)`

Ensure a loopback Ollama service is reachable and contains one model.

### Class `ModelAdapter`

Protocol for language-model adapters used by dialog controllers.

- `ModelAdapter.generate(self, prompt: str)`: Generate method for this module's MVC responsibility.
- `ModelAdapter.generate_messages(self, messages: list[ChatMessage])`: Generate from provider-neutral chat messages.

### Class `ModelParameterSet`

Data model for generation parameter presets.

### Class `ModelProviderSpec`

Selectable Agent B language-model provider metadata.

### Class `ModelProfileSpec`

Reproducible Agent B model condition independent of runtime plumbing.

### Function `available_model_profile_keys()`

Internal module operation.

### Function `research_model_profiles_by_tier()`

Return the two primary Agent B model contrasts in each size tier.

### Function `model_profile_defaults(key)`

Return provider settings for a registered model condition.

### Function `model_profile_metadata(key)`

Internal module operation.

### Function `matching_model_profile(provider, model)`

Return the registered condition matching a provider/model pair, if any.

### Function `available_model_provider_keys()`

Return the supported Agent B language-model implementations.

### Function `model_provider_description(key)`

Internal module operation.

### Function `model_provider_defaults(key)`

Return provider-specific model and endpoint defaults for configuration UIs.

### Function `get_model_parameter_set(key: str)`

Get model parameter set function for this module's MVC responsibility.

### Function `messages_to_prompt(messages: list[ChatMessage])`

Serialize chat messages for text-completion style models.

### Function `openai_messages(messages: list[ChatMessage])`

Convert neutral messages to OpenAI-compatible JSON messages.

### Class `TransformersModelAdapter`

Hugging Face transformer adapter implementing the model-generation protocol.

- `TransformersModelAdapter.__init__(self, name: str, tokenizer, model, device: str=DEVICE, max_new_tokens: int=MAX_NEW_TOKENS, max_input_tokens: int=MAX_INPUT_TOKENS, model_parameters: ModelParameterSet | None=None, max_time_sec: int | float | None=GENERATION_MAX_TIME_SEC)`: init method for this module's MVC responsibility.
- `TransformersModelAdapter.with_model_params(self, model_param_key: str)`: With model params method for this module's MVC responsibility.
- `TransformersModelAdapter.generate(self, prompt: str)`: Generate method for this module's MVC responsibility.
- `TransformersModelAdapter.generate_messages(self, messages: list[ChatMessage])`: Generate from chat messages using tokenizer chat templates when available.

### Class `OpenAICompatibleChatAdapter`

Adapter for OpenAI/ChatGPT-compatible chat-completions APIs.

- `OpenAICompatibleChatAdapter.__init__(self, model: str=CHAT_MODEL, api_key: str | None=CHAT_API_KEY, base_url: str=CHAT_BASE_URL, timeout_sec: float=CHAT_TIMEOUT_SEC, max_new_tokens: int=MAX_NEW_TOKENS, model_parameters: ModelParameterSet | None=None)`: Internal class operation.
- `OpenAICompatibleChatAdapter.with_model_params(self, model_param_key: str)`: Internal class operation.
- `OpenAICompatibleChatAdapter.generate(self, prompt: str)`: Internal class operation.
- `OpenAICompatibleChatAdapter.generate_messages(self, messages: list[ChatMessage])`: Internal class operation.

### Class `OllamaChatAdapter`

Adapter for a local Ollama `/api/chat` endpoint.

- `OllamaChatAdapter.__init__(self, model: str=OLLAMA_MODEL, base_url: str=OLLAMA_BASE_URL, timeout_sec: float=CHAT_TIMEOUT_SEC, max_new_tokens: int=MAX_NEW_TOKENS, model_parameters: ModelParameterSet | None=None)`: Internal class operation.
- `OllamaChatAdapter.with_model_params(self, model_param_key: str)`: Internal class operation.
- `OllamaChatAdapter.generate(self, prompt: str)`: Internal class operation.
- `OllamaChatAdapter.generate_messages(self, messages: list[ChatMessage])`: Internal class operation.

### Function `model_adapter_runtime_metadata(adapter, *, provider=None, profile='custom', roles=())`

Return provider-neutral audit metadata for an adapter actually used in a run.

## `coop_navigation_sds/NaturalLanguageGeneration/prompt_audit.py`

Exact, serializable provenance for language-model prompt calls.

### Function `_message_document(message)`

Internal module operation.

### Function `begin_prompt_audit(*, agent, purpose, stage, turn, messages)`

Create immutable prompt-call evidence before model generation starts.

### Function `finish_prompt_audit(audit, *, raw_output=None, cleaned_output=None, accepted=False, decision, error=None)`

Finalize one prompt audit with the model output and verifier decision.

### Function `record_prompt_delivery(audit, output, source)`

Record which guarded output was ultimately sent to text-to-speech.

### Function `consume_prompt_audits(owner)`

Return and clear an object's buffered audits without sharing mutable state.

## `coop_navigation_sds/NaturalLanguageUnderstanding/__init__.py`

Recognition transcript interpretation and semantic route extraction.

No public runtime components are defined in this module.

## `coop_navigation_sds/NaturalLanguageUnderstanding/clarification.py`

Clarification helpers for compact spoken repair turns.

### Function `route_vocabulary()`

Return words that can be clarified in the current route domain.

### Function `misunderstood_word_options(text, vocabulary=None, limit=2, threshold=0.66)`

Return one unclear word and likely intended vocabulary alternatives.

### Function `clarification_question(text, fallback, vocabulary=None)`

Build a short natural clarification question from likely word confusion.

### Function `is_clarification_request(text)`

Return whether an utterance asks the other speaker to repair hearing.

### Function `clarification_target(text)`

Return the slot or term targeted by a clarification request.

### Function `clarification_attempt_count(conversation, speaker=None, target=None)`

Count prior repair requests, optionally for one speaker and target.

### Function `clarification_signature(text)`

Return a stable signature for one spoken repair request.

### Function `clarification_was_asked(conversation, speaker, question)`

Return whether this speaker already issued the same repair request.

### Function `transcript_repair_question(corrections)`

Turn a domain-critical transcript normalization into an explicit repair turn.

### Function `is_clarification_confirmation(text)`

Return whether text is a concise answer to a hearing-repair question.

### Function `last_substantive_agent_b_utterance(conversation)`

Return the latest Agent B turn that is not a repair confirmation.

### Function `_term_from_prior_speech(heard_option, prior_spoken_text)`

Internal module operation.

### Function `clarification_confirmation(conversation, prior_spoken_text=None)`

Answer one explicit hearing repair using Agent B's own prior wording.

## `coop_navigation_sds/NaturalLanguageUnderstanding/interpreter.py`

Route interpretation model that infers connected station sequences from natural Agent B replies.

### Class `NaturalRouteInterpreter`

Infers proposed routes from ordinary spoken station mentions.

- `NaturalRouteInterpreter.station_mentions(self, text)`: Station mentions method for this module's MVC responsibility.
- `NaturalRouteInterpreter.interpret_reply(self, text, scenario)`: Interpret reply method for this module's MVC responsibility.
- `NaturalRouteInterpreter._route_from_labeled_station_sequence(self, text, label)`: Parse an explicit station sequence such as 'Stations: Alpha to Bravo'.
- `NaturalRouteInterpreter.has_station_mentions(self, text)`: Has station mentions method for this module's MVC responsibility.
- `NaturalRouteInterpreter._candidate_fragments(self, text)`: candidate fragments method for this module's MVC responsibility.
- `NaturalRouteInterpreter._valid_subroutes(self, mentions, fragment, fragment_index, scenario)`: valid subroutes method for this module's MVC responsibility.
- `NaturalRouteInterpreter._route_from_named_line_legs(self, text)`: Parse compact legs such as 'metro line M1 from Alpha to Echo'.
- `NaturalRouteInterpreter._route_from_boarding_and_lines(self, text)`: Expand compact 'Boarding' routes using the spoken line names.
- `NaturalRouteInterpreter._raw_labeled_station_mentions(self, text, label)`: Internal class operation.
- `NaturalRouteInterpreter._line_mentions(text)`: Internal class operation.
- `NaturalRouteInterpreter._line_segment_path(line_name, origin, target)`: Return the station path between two stops on the named line.
- `NaturalRouteInterpreter._expand_spoken_route(self, mentions)`: Expand compact boarding/change mentions into adjacent station paths.
- `NaturalRouteInterpreter._shortest_station_path(origin, target)`: Find the shortest station-count path between two named stops.

## `coop_navigation_sds/NaturalLanguageUnderstanding/transcript_normalization.py`

Conservative post-ASR normalization for the navigation domain.

### Function `normalize_spoken_line_codes(text)`

Repair a line code only when its transport mode supplies context.

### Function `transit_vocabulary()`

Return public names and task words that recognizers may confuse.

### Function `normalize_transit_transcript(text, threshold=0.86, vocabulary=None)`

Repair only close domain terms; never substitute the source utterance.

### Function `transcript_token_changes(source, target)`

Return case-insensitive word-level replacements, deletions, and insertions.

## `coop_navigation_sds/ResultsAndArtifacts/__init__.py`

Run directories, protocols, logs, spreadsheets, audio, and network artifacts.

No public runtime components are defined in this module.

## `coop_navigation_sds/ResultsAndArtifacts/artifacts.py`

Research artifact writers for model, network, and metric analysis data.

### Function `_json_default(value)`

Internal module operation.

### Function `execution_run_id(label=None, timestamp=None)`

Return a systematic identifier for one complete program execution.

### Function `create_execution_run_dir(base_dir, label=None, timestamp=None)`

Create and return a unique output folder for one execution run.

### Function `_metric_export_context(output_dir, scope)`

Internal module operation.

### Function `write_metrics_csv(metrics, path, context=None)`

Write flat metric rows as CSV.

### Function `write_metrics_file(metrics, path, context=None)`

Write metrics as XLSX or CSV based on the requested file extension.

### Function `write_metric_phase_logs(metrics, output_dir, *, result_scope='run')`

Write calculated phase metrics to one JSONL file and a matching catalog.

### Function `write_network_research_artifacts(current_time_min, output_dir, picture_dir=None)`

Write network data JSON and SVG graph artifacts.

### Function `write_conversation_transcript(path, result, turns, speech_turns, timing_turns, phase_timings=None)`

Write the full human-readable dialog transcript for one conversation.

### Function `write_conversation_wav(path, speech_turns, gap_sec=0.15)`

Compile per-turn WAV artifacts into one conversation WAV when possible.

### Function `remove_compiled_turn_audio_sources(audio_manifest, roots)`

Remove per-turn speech files after a complete conversation WAV is compiled.

### Function `is_path_relative_to(path, root)`

Internal module operation.

### Function `remove_empty_parent_dirs(start, roots)`

Remove empty generated audio folders without climbing outside safe roots.

### Function `build_combined_protocol(result, summary, turns, speech_turns, timing_turns, phase_timings, nlu_turns, agent_turn_segments, agent_timing_summary, runtime_events, candidate_events, verification, audio_manifest)`

Return a single JSON document containing all protocol data for one run.

### Function `build_metric_input_document(result, scenario)`

Build the immutable evidence document used by retrospective metrics.

### Function `write_metric_inputs(result, scenario, path)`

Persist raw run evidence before any derived metric is calculated.

### Function `calculate_metrics_from_inputs(path)`

Recalculate one metric record entirely from a stored evidence document.

### Function `write_batch_metric_inputs(results, path)`

Persist every completed condition before calculating batch metrics.

### Function `calculate_batch_metrics_from_inputs(path)`

Reconstruct batch metrics solely from persisted immutable evidence.

### Function `build_retrospective_metrics_document(metrics, *, result_scope, input_inventory=None)`

Return a common retrospective metric document for single and batch runs.

### Function `write_retrospective_metrics_json(metrics, path, *, result_scope, input_inventory=None)`

Write common retrospective metric evidence for single or batch runs.

### Function `write_conversation_protocol(result, output_dir)`

Write detailed, parsable protocol data for one completed conversation.

### Function `write_single_run_research_outputs(result, scenario, output_dir, *, run_dir=None)`

Write protocol files plus compiled metric files for one interactive run.

### Function `write_conversation_protocols(results, output_dir)`

Write protocol artifacts for all completed conversations.

### Function `write_jsonl(path, rows)`

Internal module operation.

### Function `write_standard_run_summary(results, metrics, output_dir, *, result_scope, manifest_path)`

Write the common human/machine entry point for single and batch runs.

### Function `write_failure_indicator_report(metrics, path, minimum_per_outcome=3)`

Persist leakage-controlled exploratory failure thresholds for a batch.

### Function `verify_conversation_protocol(result, turns, speech_turns, timing_turns, nlu_turns, agent_turn_segments=None, agent_timing_summary=None, runtime_events=None)`

Return validation checks for research-grade protocol completeness.

### Function `write_experiment_manifest(conditions, output_dir, *, num_turns, speech_engine, speech_scope, agent_b_plugin, tts_engine=None, asr_engine=None, metrics_filename='metrics.xlsx', configuration=None)`

Write a scientific-method manifest describing the experiment design.

## `coop_navigation_sds/ResultsAndArtifacts/comparison.py`

Combine completed experiment runs into portable comparison tables and charts.

### Function `discover_run_directories(paths)`

Return unique standard run directories found at or below ``paths``.

### Function `_read_jsonl(path)`

Internal module operation.

### Function `_read_csv(path)`

Internal module operation.

### Function `_write_csv(path, rows)`

Internal module operation.

### Function `_number(value)`

Internal module operation.

### Function `load_comparison_data(run_directories)`

Load and join canonical condition and long-form metric records.

### Function `summarize_metrics(metrics)`

Aggregate each numeric metric by run and experimental component tuple.

### Function `calculate_run_deltas(summaries)`

Calculate pairwise mean differences for matching metrics and run types.

### Function `identify_metric_outliers(metrics, conditions, minimum_samples=5, modified_z_threshold=3.5)`

Find robust pre-outcome metric outliers and relate them descriptively to task
outcomes without using outcome-phase metrics as predictors.

### Function `summarize_run_outcomes(conditions, outliers=())`

Summarize condition-level task outcomes and aligned metric signals per run.

### Function `summarize_metric_indicators(outliers)`

Aggregate which pre-outcome metric outliers align with observed success or
failure, retaining contradictory evidence.

### Function `_selected_chart_rows(summaries, limit=12)`

Internal module operation.

### Function `_bar_svg(rows)`

Internal module operation.

### Function `write_html_report(path, run_directories, conditions, summaries, deltas, run_outcomes, outliers, metric_indicators)`

Write a dependency-free color-coded HTML report with run outcomes, metric
outlier alignment, and inline SVG comparisons.

### Function `compare_runs(inputs, output_directory)`

Build a complete comparison dataset and return its artifact paths.

### Function `main(argv=None)`

Internal module operation.

## `coop_navigation_sds/ResultsAndArtifacts/logging.py`

Background session logging for program monitoring and conversation metrics.

### Class `StructuredEvent`

Base structured event for monitored sessions.

- `StructuredEvent.to_dict(self)`: Internal class operation.

### Class `ProgramSegmentEvent`

Structured event for a monitored program segment.

- `ProgramSegmentEvent.__init__(self, session_id, timestamp, name, payload=None)`: Internal class operation.

### Class `ConversationStepEvent`

Structured event for a single conversation step.

- `ConversationStepEvent.__init__(self, session_id, timestamp, name, payload=None)`: Internal class operation.

### Class `SystemEvent`

Structured event for generic session events.

- `SystemEvent.__init__(self, session_id, timestamp, name, payload=None)`: Internal class operation.

### Function `_normalize_profile(profile: str | None)`

Internal module operation.

### Function `resource_snapshot()`

Return lightweight resource usage information for the current process.

### Class `SessionLogger`

Write structured session logs on a background thread.

- `SessionLogger.__init__(self, session_name: str, log_dir: str | Path=RESULTS_DIR, profile: str=LOG_PROFILE_FULL)`: Internal class operation.
- `SessionLogger.submit(self, event: StructuredEvent)`: Submit a structured event to the background writer.
- `SessionLogger.segment(self, name: str, **payload)`: Log a program segment with start/end timestamps and resource usage.
- `SessionLogger.log_step(self, *, turn: int, speaker: str, utterance: str, metrics: dict[str, Any])`: Record a single conversation step with metrics and resource usage.
- `SessionLogger.log_metric_snapshot(self, snapshot: dict[str, Any])`: Record a periodic research metric snapshot.
- `SessionLogger.close(self)`: Flush and stop the background writer.
- `SessionLogger._run(self)`: Internal class operation.
- `SessionLogger._format_summary(event: dict[str, Any])`: Internal class operation.
- `SessionLogger._should_log_segment(self, name: str)`: Internal class operation.
- `SessionLogger._should_log_runtime(self)`: Internal class operation.
- `SessionLogger.accepts_tuple_event(self, event)`: Internal class operation.

### Class `MonitoringEventQueue`

Forward UI-compatible tuple events to a UI queue and structured logger.

- `MonitoringEventQueue.__init__(self, ui_queue, logger: SessionLogger)`: Internal class operation.
- `MonitoringEventQueue.put(self, event)`: Internal class operation.
- `MonitoringEventQueue.emit_structured(self, event: StructuredEvent)`: Internal class operation.
- `MonitoringEventQueue.segment(self, name: str, **payload)`: Internal class operation.
- `MonitoringEventQueue.log_step(self, *, turn: int, speaker: str, utterance: str, metrics: dict[str, Any])`: Internal class operation.
- `MonitoringEventQueue.log_metric_snapshot(self, snapshot: dict[str, Any])`: Internal class operation.
- `MonitoringEventQueue.close(self)`: Internal class operation.
- `MonitoringEventQueue._to_structured_event(event)`: Internal class operation.

## `coop_navigation_sds/ResultsAndArtifacts/metric_tables.py`

Canonical long-form metric tables for plotting, joins, and audit.

### Function `_context_columns(context=None)`

Internal module operation.

### Function `metric_wide_rows(records, context=None)`

Return one normalized condition-level row per metric record.

### Function `metric_long_rows(records, context=None)`

Return one normalized row per condition and metric.

### Function `_write_table_exports(rows, csv_path, jsonl_path)`

Internal module operation.

### Function `write_metric_long_exports(records, output_dir, context=None)`

Write matching long and wide metric datasets for graphing and joins.

## `coop_navigation_sds/ResultsAndArtifacts/speech_matrix.py`

Cartesian speech-backend verification and research protocol output.

### Class `SpeechMatrixCase`

Result of one text-to-speech and recognition pairing.

### Function `_module_available(module_name)`

Internal module operation.

### Function `_configured_path(value)`

Internal module operation.

### Function `_isolated_python(engine, explicit, environment_dir)`

Internal module operation.

### Function `_provider_import_ready(engine, explicit, environment_dir, module_name)`

Internal module operation.

### Function `tts_live_readiness(engine, config)`

Return whether an actual synthesis attempt is locally configured.

### Function `asr_live_readiness(engine, config)`

Return whether an actual recognition attempt is locally configured.

### Function `_write_probe_wave(path)`

Internal module operation.

### Function `_contract_synthesizer(audio_dir, engine_name)`

Internal module operation.

### Function `_contract_transcriber(engine_name)`

Internal module operation.

### Function `_pipeline_config(base_config, tts_engine, asr_engine, audio_dir)`

Internal module operation.

### Function `_run_contract_case(case, config)`

Internal module operation.

### Function `_run_live_case(case, config)`

Internal module operation.

### Function `run_speech_backend_matrix(output_dir, base_config=None, run_live=False)`

Execute every registered TTS/ASR pairing and write its protocol.

### Function `_matrix_summary(cases)`

Internal module operation.

### Function `_write_matrix_protocol(output_dir, protocol)`

Internal module operation.

## `coop_navigation_sds/ResultsAndArtifacts/xlsx.py`

Small XLSX writer for dependency-free research metric exports.

### Function `write_metrics_xlsx(metrics, path, context=None)`

Write metric records to an XLSX workbook with summary and per-phase sheets.

### Function `_summary_rows(records, context=None)`

Internal module operation.

### Function `_phase_rows(records, phase, context=None)`

Internal module operation.

### Function `_worksheet_xml(rows)`

Internal module operation.

### Function `_cell_xml(cell_ref, value)`

Internal module operation.

### Function `_excel_column(index)`

Internal module operation.

### Function `_sheet_name(name)`

Internal module operation.

### Function `_content_types_xml(sheet_count)`

Internal module operation.

### Function `_root_rels_xml()`

Internal module operation.

### Function `_workbook_xml(sheet_names)`

Internal module operation.

### Function `_workbook_rels_xml(sheet_count)`

Internal module operation.

## `coop_navigation_sds/smoke.py`

Fast deterministic end-to-end experiment used by local and CI checks.

### Function `smoke_run_config(results_dir='results')`

Return a dependency-light configuration that traverses every runtime phase.

### Function `run_smoke(results_dir='results', event_queue=None)`

Run the deterministic pipeline and return its result and artifact paths.

### Function `main(argv=None)`

Internal module operation.

## `coop_navigation_sds/TextToSpeech/__init__.py`

Text synthesis engines, audio personas, and provider setup.

No public runtime components are defined in this module.

## `coop_navigation_sds/TextToSpeech/base16384_compat.py`

Minimal pure-Python Base16384 codec used when the native wheel is unavailable.

### Function `encode_to_string(data)`

Encode bytes using the Base16384 representation consumed by ChatTTS.

### Function `decode_from_string(data)`

Decode a Base16384 string into bytes.

## `coop_navigation_sds/TextToSpeech/engines.py`

Public text-to-speech component API.

No public runtime components are defined in this module.

## `coop_navigation_sds/TextToSpeech/personas.py`

Reusable, named synthesis profiles for controlled speech experiments.

### Function `audio_persona_keys(role=None)`

Return stable profile keys, optionally filtered by intended role.

### Function `get_audio_persona(key, fallback=DEFAULT_AGENT_A_AUDIO_PERSONA)`

Return a copy of a configured profile.

### Function `synthesis_values(key, fallback=DEFAULT_AGENT_A_AUDIO_PERSONA)`

Return only fields accepted by the synthesis pipeline.

### Function `describe_audio_persona(key, fallback=DEFAULT_AGENT_A_AUDIO_PERSONA)`

Return compact GUI and manifest text for a profile.

## `coop_navigation_sds/TextToSpeech/setup.py`

Provider-environment catalog and setup utilities.

### Class `ProviderProfile`

Class component; see its typed members and call sites.

## `coop_navigation_sds/TransportNetwork/__init__.py`

Transit network, route constraints, scenarios, and route planning.

No public runtime components are defined in this module.

## `coop_navigation_sds/TransportNetwork/constraints.py`

Constraint-aware route baselines for comparing agent proposals.

### Function `normalize_objective_mode(value)`

Return a supported Agent A objective mode.

### Class `RouteConstraintProfile`

Normalized persona route preferences used for scoring candidate routes.

- `RouteConstraintProfile.from_persona(cls, persona, scenario=None)`: Internal class operation.
- `RouteConstraintProfile.label(self)`: Internal class operation.

### Class `ConstraintRoute`

Best route under a persona's stated constraints.

### Function `probability_class(value)`

Map an internal probability to a spoken risk class.

### Function `probability_class_allowed(value, threshold)`

Return whether a probability falls within the configured risk class.

### Function `route_risk_viability(steps, profile)`

Return class-based route viability for delay and transfer risk.

### Function `route_allowed_modes(scenario, persona=None)`

Return a hashable route-access profile for tickets and walking.

### Function `dialog_route_allowed_modes(scenario, persona=None, stated_keys=())`

Return access rules Agent A has revealed at the current dialog stage.

### Function `route_walking_minutes(steps)`

Return cumulative walking travel time.

### Function `route_average_fullness(steps)`

Internal module operation.

### Function `route_near_capacity_count(steps)`

Return the number of route segments that are near capacity.

### Function `route_has_near_capacity(steps)`

Return whether any route segment is near capacity.

### Function `route_delay_probability(steps)`

Return route-level delay risk as the maximum segment risk.

### Function `route_transfer_miss_probability(steps)`

Return route-level transfer-miss risk as the maximum transfer risk.

### Function `available_agent_a_constraints(persona, scenario)`

Return progressive Agent A constraints in the order they should be revealed.

### Function `stated_constraint_keys(conversation)`

Infer which constraints Agent A has explicitly stated so far.

### Function `route_constraint_status(steps, persona, scenario, stated_keys, transfer_tolerance=1, constraint_route=None)`

Evaluate a route against only the constraints Agent A has stated.

### Function `unsatisfied_constraint_keys(statuses)`

Return stated constraint keys whose status is not satisfied.

### Function `optimal_route_duration_min(scenario, persona=None)`

Return the fastest route before private constraints are revealed.

### Function `acceptable_duration_ratio(scenario)`

Return the configured strict duration ratio for stage-one acceptance.

### Function `acceptable_duration_limit(scenario, persona=None, constraint_route=None, slack_min=None)`

Return the maximum whole-minute duration under the configured optimal-route ratio.

### Function `stage_suboptimal_options_required(scenario)`

Return whether each conversation stage must have suboptimal viable alternatives.

### Function `minimum_stage_suboptimal_options(scenario)`

Return the configured number of non-best viable alternatives required per stage.

### Function `stage_route_options(scenario, persona, stated_keys=(), transfer_tolerance=1, limit=80)`

Return viable route options for one staged goal state.

### Function `stage_constraint_sort_key(duration_min, steps, stated_keys=())`

Rank revealed constraints before time, newest constraint first.

### Function `layered_optimal_routes(scenario, persona, transfer_tolerance=1, max_constraints=3)`

Calculate validity, time, and distinct progressive constraint optima.

### Function `stage_viability_report(scenario, persona, transfer_tolerance=1, max_constraints=2)`

Return whether each staged conversation goal has required alternative routes.

### Function `constraint_request_text(key, persona=None, scenario=None)`

Return one natural Agent A utterance fragment for a constraint key.

### Function `constraint_sort_key(duration_min, steps, profile, objective_mode=OBJECTIVE_SHORTEST_WITH_CONSTRAINTS)`

Internal module operation.

### Function `optimal_constraint_route(scenario, persona, limit=50, objective_mode=None)`

Compute the best startup baseline route under persona constraints.

### Function `ranked_constraint_routes(scenario, persona, limit=6, objective_mode=None)`

Return valid routes sorted by the persona's scientific comparison profile.

### Function `route_constraint_gap(steps, duration_min, constraint_route)`

Return scalar proposal gaps from the constraint-aware startup baseline.

## `coop_navigation_sds/TransportNetwork/network.py`

Transit network model.

### Function `grid_dimensions(num_stations: int)`

Grid dimensions function for this module's MVC responsibility.

### Function `station_grid(stations)`

Station grid function for this module's MVC responsibility.

### Function `generate_station_positions(stations)`

Generate station positions function for this module's MVC responsibility.

### Function `valid_sequence(seq)`

Valid sequence function for this module's MVC responsibility.

### Function `perimeter_ring(grid, rows, cols)`

Perimeter ring function for this module's MVC responsibility.

### Function `row_sequence(grid, r)`

Row sequence function for this module's MVC responsibility.

### Function `column_sequence(grid, rows, c)`

Column sequence function for this module's MVC responsibility.

### Function `diagonal_down_right_sequences(grid, rows, cols)`

Diagonal down right sequences function for this module's MVC responsibility.

### Function `diagonal_down_left_sequences(grid, rows, cols)`

Diagonal down left sequences function for this module's MVC responsibility.

### Function `make_candidate_lines(stations)`

Make candidate lines function for this module's MVC responsibility.

### Function `_choose_legacy_lines(stations, seed=None)`

Choose lines function for this module's MVC responsibility.

### Function `station_public_mode_map(stations, seed=0)`

Assign exactly two public transport modes to every station.

### Function `choose_lines(stations, seed=None)`

Build deterministic multimodal services with two public modes per station.

### Function `ensure_core_service(lines, stations)`

Add a compact all-station service when generated alternatives miss coverage.

### Function `apply_line_fullness(lines, seed=None)`

Attach deterministic current fullness percentages to each line.

### Function `build_station_profiles(stations, lines, seed=None)`

Create deterministic station demand profiles used for time-varying crowding.

### Function `build_station_transfer_times(stations, lines, seed=None)`

Create deterministic transfer-time variation by station.

### Function `gaussian_peak(minute, center_min, spread_min)`

Return a smooth bell-shaped multiplier around a target minute.

### Function `station_fullness_percent(station, current_time_min)`

Return the current station crowding percentage.

### Function `line_fullness_percent(line_name, current_time_min)`

Return average line crowding based on the current station demand profile.

### Function `segment_fullness_percent(line_name, station_a, station_b, current_time_min)`

Return crowding for a traversed segment using local station and line load.

### Function `is_near_capacity(fullness_percent, threshold=NEAR_CAPACITY_THRESHOLD_PERCENT)`

Return whether a fullness value is near capacity.

### Function `capacity_status(fullness_percent, threshold=NEAR_CAPACITY_THRESHOLD_PERCENT)`

Return the binary capacity label used for dialog and research outputs.

### Function `fullness_class(fullness_percent)`

Return the line-level fullness class.

### Function `line_delay_probability_class(line_name)`

Return a general delay-probability class for one line.

### Function `delay_class_probability(delay_class)`

Map a line delay class to the internal numeric risk proxy.

### Function `station_transfer_time_min(station)`

Return the station-specific line-change time.

### Function `apply_line_overrides(lines, stations)`

Apply line overrides function for this module's MVC responsibility.

### Function `line_stop_pairs(line_name, data)`

Line stop pairs function for this module's MVC responsibility.

### Function `line_segment_key(line_name, station_a, station_b)`

Return the canonical key for line-specific segment data.

### Function `map_distance(station_a, station_b)`

Return schematic map distance between two stations.

### Function `travel_time_for_line(line_name, station_a, station_b, rng=None)`

Scale travel time by schematic distance and transport mode.

### Function `generate_travel_times(lines, seed=None)`

Generate line-specific travel times.

### Function `build_edges_and_adjacency(stations, lines, travel_times)`

Build edges and adjacency function for this module's MVC responsibility.

### Function `reachable_stations(start, adjacency)`

Reachable stations function for this module's MVC responsibility.

### Function `connected_components(stations, adjacency)`

Connected components function for this module's MVC responsibility.

### Function `is_reachable(start, destination)`

Is reachable function for this module's MVC responsibility.

### Function `is_fully_connected()`

Is fully connected function for this module's MVC responsibility.

### Function `assert_fully_connected()`

Assert fully connected function for this module's MVC responsibility.

### Function `assert_station_mode_invariant()`

Fail fast when generated public service modes violate the experiment design.

### Function `rebuild_network(seed=NETWORK_SEED, force=False)`

Rebuild mutable network data for a reproducible experiment seed.

### Function `line_segment_text(line_name)`

Line segment text function for this module's MVC responsibility.

### Function `compact_network_text()`

Compact network text function for this module's MVC responsibility.

### Function `compact_line_service_text()`

Return compact line-service context.

### Function `compact_station_transfer_text(limit=10)`

Return compact transfer-time context for the longest transfer stations.

### Function `compact_line_fullness_text(current_time_min=START_TIME_MIN)`

Compact current line crowding text for prompts.

### Function `compact_station_crowding_text(current_time_min=START_TIME_MIN, limit=8)`

Return the currently busiest stations as compact prompt context.

### Function `line_stop_sequence_text(line_name, data)`

Line stop sequence text function for this module's MVC responsibility.

### Function `compact_travel_time_text()`

Compact travel time text function for this module's MVC responsibility.

## `coop_navigation_sds/TransportNetwork/overview.py`

Structured transit-network data for views and tests.

### Class `NetworkLineRow`

Class component; see its typed members and call sites.

### Class `NetworkStationRow`

Class component; see its typed members and call sites.

### Class `NetworkOverview`

Class component; see its typed members and call sites.

### Function `build_network_overview(current_time_min)`

Build complete network rows without view-layer calculations.

## `coop_navigation_sds/TransportNetwork/picture.py`

Dependency-free network graph picture export.

### Function `_connection_records()`

Return every line-specific connection with a non-overlapping lane.

### Function `_line_styles()`

Assign a unique color-pattern combination to every service.

### Function `_line_style(line_name, styles)`

Internal module operation.

### Function `write_network_svg(path, *, title='CoopNavigationSDS transit network')`

Write every realized connection and an external line index to SVG.

## `coop_navigation_sds/TransportNetwork/routes.py`

Route-planning model for schedules, travel time, transfer handling, route validation, and optimal path search.

### Function `fmt_time(minutes: int)`

Fmt time function for this module's MVC responsibility.

### Function `normalize_allowed_modes(allowed_modes=None)`

Return enabled public and walking modes from a route access profile.

### Function `maximum_walking_minutes(allowed_modes=None)`

Extract the cumulative walking limit encoded in an access profile.

### Function `line_mode(line_name)`

Return the transport mode for a service line.

### Function `step_transport_type(step)`

Return a stored mode, or reconstruct it when the active network permits.

### Function `line_allowed(line_name, allowed_modes=None)`

Return whether a service is covered by the caller's access profile.

### Function `transfer_time_at_station(station, previous_line, next_line, default_transfer_time_min)`

Return transfer time only when the route changes lines.

### Function `transfer_miss_probability(station, next_line, transfer, wait, current_time_min)`

Estimate the risk of missing a connection after a line change.

### Function `next_departure(current_time_min: int, line_name: str)`

Next departure function for this module's MVC responsibility.

### Function `line_direction_sequences(line_name: str)`

Line direction sequences function for this module's MVC responsibility.

### Function `segment_travel(a: str, b: str)`

Segment travel function for this module's MVC responsibility.

### Function `segment_travel_on_line(line_name: str, a: str, b: str)`

Return travel time for a specific line segment.

### Function `line_direction_key(line_name: str, station: str, nxt: str)`

Line direction key function for this module's MVC responsibility.

### Function `line_departure_offset(line_name: str, station: str, nxt: str)`

Line departure offset function for this module's MVC responsibility.

### Function `next_train_departure(current_time_min: int, line_name: str, station: str, nxt: str)`

Next train departure function for this module's MVC responsibility.

### Function `segment_delay_probability(line_name: str, station_a: str, station_b: str, depart_min: int)`

Estimate operational delay risk for one traversed segment.

### Function `optimal_time_route(start: str, goal: str, start_time_min: int, transfer_time_min: int, allowed_modes=None)`

Optimal time route function for this module's MVC responsibility.

### Function `candidate_time_routes(start: str, goal: str, start_time_min: int, transfer_time_min: int, limit: int=4, max_extra_stops: int=4, max_paths: int=4000, allowed_modes=None)`

Return distinct valid route candidates, searching shorter station paths first.

### Function `route_station_sequence(steps)`

Route station sequence function for this module's MVC responsibility.

### Function `route_path_text_from_steps(steps)`

Return a concise station/line path with same-line stops condensed.

### Function `route_text_from_steps(steps)`

Return a concise spoken route grouped by boarding and change points.

### Function `route_step_details(steps)`

Return stable analysis fields for every station-to-station route edge.

### Function `route_rides(steps)`

Group adjacent steps that stay on the same line into spoken ride legs.

### Function `route_boarding_route(steps)`

Return start, transfer boarding points, and destination for compact speech.

### Function `route_duration_breakdown(steps)`

Route duration breakdown function for this module's MVC responsibility.

### Function `route_line_sequence(steps)`

Route line sequence function for this module's MVC responsibility.

### Function `route_line_change_count(steps)`

Return the number of line changes in a scheduled route.

### Function `route_duration_text(steps)`

Route duration text function for this module's MVC responsibility.

### Function `route_is_valid(stations, allowed_modes=None)`

Route is valid function for this module's MVC responsibility.

### Function `estimate_route_time(stations, start_time_min, transfer_time_min, allowed_modes=None)`

Estimate route time function for this module's MVC responsibility.

## `coop_navigation_sds/TransportNetwork/scenarios.py`

Scenario model definitions for start/destination/time/transfer settings used by dialogs and experiments.

### Function `make_scenario(name, start_station, destination_station, destination_stations=None, start_time_min=START_TIME_MIN, transfer_time_min=TRANSFER_TIME_MIN, max_transfer_miss_probability=MAX_TRANSFER_MISS_PROBABILITY, max_delay_probability=MAX_ROUTE_DELAY_PROBABILITY, acceptable_duration_ratio=ACCEPTABLE_DURATION_RATIO, min_stage_suboptimal_options=MIN_STAGE_SUBOPTIMAL_OPTIONS, require_stage_suboptimal_options=REQUIRE_STAGE_SUBOPTIMAL_OPTIONS, require_constraint_route_changes=True, allow_unreachable=False)`

Make scenario function for this module's MVC responsibility.

### Function `_station_at(index_spec)`

Resolve an index specification into a station name.

### Function `_stations_at(index_specs)`

Internal module operation.

### Function `get_scenario(scenario_key: str)`

Get scenario function for this module's MVC responsibility.

## `coop_navigation_sds/TransportNetwork/station_names.py`

Station-name model.

### Function `get_station_names(count: int)`

Get station names function for this module's MVC responsibility.

## `coop_navigation_sds/TransportNetwork/test_cases.py`

Standardized test-case model for scenario/persona combinations and opening utterances.

### Class `StandardizedTestCase`

Experiment test-case model binding scenario, persona, and opening utterance behavior.

- `StandardizedTestCase.persona(self)`: Persona method for this module's MVC responsibility.
- `StandardizedTestCase.scenario(self)`: Scenario method for this module's MVC responsibility.
- `StandardizedTestCase.opening_utterance(self)`: Opening utterance method for this module's MVC responsibility.
- `StandardizedTestCase.with_persona(self, persona_key: str)`: With persona method for this module's MVC responsibility.
- `StandardizedTestCase.with_scenario_overrides(self, **values)`: Return a copy whose runtime policy values override scenario defaults.

### Function `get_test_case(test_case_key: str)`

Get test case function for this module's MVC responsibility.
