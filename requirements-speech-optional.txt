# Speech providers supported in the Python 3.14 project runtime.
ChatTTS==0.2.5
faster-whisper==1.2.1
piper-tts==1.4.2
qwen-asr==0.0.6
sherpa-onnx==1.13.3
vosk==0.3.45

# System package: eSpeak NG (`espeak-ng` executable).
# Coqui uses `coqui-tts==0.27.5` in the isolated Python 3.11 provider created
# by `scripts/setup_speech_providers.py`; do not install it in Python 3.14.
